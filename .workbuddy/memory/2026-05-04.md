
## 为 cbz2pdf.py 编写三语言文档 (v1.0)

### 完成内容
1. **中文文档** `docs/scripts/cbz2pdf.md` — 完整功能说明、用法示例、参数表格、运行流程、注意事项
2. **英文文档** `docs/en/scripts/cbz2pdf.md` — 同上（英译）
3. **日文文档** `docs/ja/scripts/cbz2pdf.md` — 同上（日译）
4. **config.ts 更新**：
   - keywords 追加 `cbz2pdf`
   - 三语言 sidebar 追加 `cbz2pdf.py` 条目
5. **重新构建** — `npm run docs:build` 成功，36.29s 完成

### 文档要点
- 版本：v1.0（首个版本）
- 功能：CBZ→PDF 转换、交互/非交互双模式、自定义命名格式、并行转换(1-8)、跳过已存在
- 命名格式变量：`{name}` `{index}` `{date}`
- 命令行参数：`-i` `-o` `--keep-name` `--name-format` `-j` `-y`

## 修改 cbz2pdf.py 和 download_cbz_interactive.py 文档格式

### 修改内容
1. **cbz2pdf.py 三语言文档**：
   - 版本号位置：从独立一行 `<Badge>` 改为行内 `当前版本<Badge>`（与其他脚本一致）
   - 使用方法：新增「下载脚本」子节（CNB/GitHub wget 链接）
2. **download_cbz_interactive.py 三语言文档**：
   - 依赖部分：从列表格式改为表格格式（仿照 cbz2pdf.py 的表格）
3. 重新构建文档成功（14.63s）
